import './bootstrap';
import { library } from '@fortawesome/fontawesome-svg-core';
import { fas } from '@fortawesome/free-solid-svg-icons';
import { FontAwesomeIcon } from '@fortawesome/vue-fontawesome';
import { createApp } from 'vue';


library.add(fas);

// Esta é uma suposição, já que não posso ver o restante do seu código.
// Você precisa identificar onde o seu aplicativo Vue está sendo instanciado
// e então adicionar o componente FontAwesome ao mesmo.
//onst app = createApp(App);
//app.component('fa', FontAwesomeIcon);

//app.mount('#app');
